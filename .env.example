# =============================================================================
# HMS Frontend Environment Configuration
# =============================================================================
# Copy this file to .env or .env.local and adjust values.
# =============================================================================

# Backend API base URL (must match HMS.API launch URL, e.g. launchSettings.json http profile)
VITE_BASE_URL=http://localhost:5048

# Optional: Google Maps (MapPicker)
# VITE_GOOGLE_MAPS_API_KEY=
